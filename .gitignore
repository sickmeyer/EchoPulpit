config.yaml
.env.local
cookies.txt
output/
models/
*.db
*.db-shm
*.db-wal
__pycache__/
*.pyc
.pytest_cache/

# Rendered IAM policies (contain real account IDs/resource names) and
# packaged Lambda zips produced by deploy/setup.sh -- never commit these.
deploy/.build/
*.pkr.hcl.bak


# RVR1960 full text is copyrighted: build it locally (scripts/build_rv1960.py)
# and upload it to the private artifacts bucket only. Never commit it.
data/rv1960.json
